import { Router } from 'express';
// import * as purchaseOrderController from '../controllers/purchaseOrderController';

const router = Router();

// ALL PURCHASE ORDER ROUTES TEMPORARILY DISABLED DUE TO MISSING HANDLERS
// TODO: Implement missing handlers in purchaseOrderController.ts

export default router;
